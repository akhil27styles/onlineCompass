---
import SiteHeader from './SiteHeader.astro';
import ToolTabs from './ToolTabs.astro';
import LocationBar from './LocationBar.astro';

const { current = 'compass' } = Astro.props as { current?: 'compass' | 'sun' | 'moon' | 'solar-system' | 'qibla' | 'wind' | 'golden-hour' | 'uv-index' | 'prayer-times' | 'day-length' };
---

<SiteHeader current={current} />
<ToolTabs current={current} />
<LocationBar />
